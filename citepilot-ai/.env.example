# CitePilot AI Environment Configuration
GOOGLE_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash-lite
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info
# Jev (TypeSafe System One) decision layer — opt-in, disabled when empty (ADR-012).
# Get a key at https://console.typesafe.ai/keys. Never commit real keys.
TYPESAFE_API_KEY=
TYPESAFE_MODEL=jev-latest
TYPESAFE_ENABLED=true
TYPESAFE_AUTO_ACCEPT=0.8
