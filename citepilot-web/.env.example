# For local dev — use localhost
# For Vercel deployment — override with deployed gateway URL
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# Production gateway (set in Vercel env vars):
# NEXT_PUBLIC_API_URL=https://citepilot-gateway.up.railway.app/api/v1
