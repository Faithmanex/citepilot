export default function Footer() {
  return (
    <footer className="border-t-2 border-rule py-7.5">
      <div className="max-w-[1080px] mx-auto px-8 flex justify-between items-center flex-wrap gap-3.5 font-type text-[13px] font-semibold text-ink-faint">
        <span>
          CitePilot — AI-powered academic citation consistency checker.
        </span>
        <span>CitePilot Engine v2.0</span>
      </div>
    </footer>
  );
}
